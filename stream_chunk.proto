// A chunk of data for a streaming request
//
// ### Response
//
// * `content`: The bytes
// * `EOF`: indicates whether the stream is finished. An EOF chunk should not contain content.
//

package warden;

message StreamChunk {
  optional bytes content = 1;
  optional bool EOF = 2;
}
